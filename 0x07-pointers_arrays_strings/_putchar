#include "main.h"
#include <unistd.h>

/**
 * _putchar - this will writes the character c to stdout
 * @c: character to be printed
 *
 * Return: On success 1.
 * When error, -1 is then returned, errno is set appropriately.
 */
int _putchar(char c)
{
	return (write(1, &c, 1));
}
