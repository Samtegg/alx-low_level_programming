#include "main.h"


/**
 * _abs - this computes the absolute value of an int
 * @n: int to be check
 * Return: the absolute value of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
