#include "main.h"

/**
 * add -This will add two integers then returns the result
 * @a: int added to b
 * @b: int added to a
 * Return: total of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
