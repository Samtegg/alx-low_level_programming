#include "main.h"

/**
 * _abs - This will compute the absolute value of the integer
 * @n: int to be checked
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
